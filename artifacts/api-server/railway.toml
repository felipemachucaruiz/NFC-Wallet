[build]
buildCommand = "pnpm install && pnpm run build && cp -r ../../loadtests/k6 dist/k6scripts"

[deploy]
startCommand = "pnpm run start"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 30
releaseCommand = "cd ../../lib/db && pnpm drizzle-kit migrate --config ./drizzle.config.ts"
